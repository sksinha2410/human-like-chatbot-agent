# Human-Like Chatbot Agent - GitHub Pages Frontend

This is the GitHub Pages hosted frontend for the Human-Like Chatbot Agent.

## Usage

1. **Visit the hosted page**: The frontend is available at `https://YOUR_USERNAME.github.io/human-like-chatbot-agent/`
   (Replace `YOUR_USERNAME` with your GitHub username)

2. **Configure the API endpoint**:
   - Click the ⚙️ Settings button in the header
   - Enter the URL of your deployed backend API (e.g., `https://your-api.render.com`)
   - Click "Save & Connect"

3. **Start chatting**: Once connected, you can start testing the chatbot's capabilities!

## Deploying Your Own Backend

The chatbot requires a backend server to function. You can deploy the backend using:

- **Render**: Free tier available, easy deployment from GitHub
- **Railway**: Simple deployment with auto-scaling
- **Vercel**: Serverless deployment option
- **AWS/GCP/Azure**: For production workloads

See the main [README](../README.md) for detailed deployment instructions.

## Local Testing

To test the frontend locally:

1. Start your backend server (see main README)
2. Open `index.html` in your browser
3. Configure the API endpoint to `http://localhost:3000`

## Features

This frontend demonstrates:
- ✅ Clean, modern chat interface
- ✅ Configurable API endpoint
- ✅ Persistent API URL storage (localStorage)
- ✅ Real-time typing indicators
- ✅ Error handling and connection status
- ✅ Responsive design
- ✅ Mobile-friendly UI
